{
  "name": "remie",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd frontend && npm ci && npm run build && cd ../functions && npm ci && npm run build",
    "build:frontend": "cd frontend && npm ci && npm run build",
    "build:functions": "cd functions && npm ci && npm run build"
  },
  "dependencies": {
    "firebase-tools": "^14.27.0"
  }
}
